# alisherFarhadi.com

This is one of two repositories created for my portfolio website <a href="https://alisherFarhadi.com/"><samp>alisherFarhadi.com</samp></a>. If you are looking for the 2D OS repository you can find [...]

<br>

To setup a dev environment:

```bash
# Clone the repository

# Install dependencies 
npm i

# Run the local dev server
npm run dev
```

To serve a production build:

```bash
# Install dependencies if not already done - 'npi i'

# Build for production
npm run build

# Serve the build using express
npm start
```
